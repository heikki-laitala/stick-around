{
  "name": "stick-around-overlay",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "eslint": "^10.8.1",
    "vitest": "^4"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
